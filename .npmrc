# pnpm: node_modules estricto. Solo se resuelve lo declarado en package.json.
# Esto elimina las "dependencias fantasma" (usar algo que no declaraste).
node-linker=isolated
hoist=false
shamefully-hoist=false
strict-peer-dependencies=true
auto-install-peers=false
